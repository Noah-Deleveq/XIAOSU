# 自评

## 做了什么

「小苏」是给公司员工用的内部 AI 助手：员工在钉钉 / 飞书里 @小苏，就能问年假、报销、考勤等制度问题，或查员工 / 考勤 / 销售订单；答案带原文引用，查不到会明确拒答。管理员在 Web 后台维护知识库、看对话日志、Token 成本与请求链路，并切换 LLM 供应商。

## 完成情况

- 知识库：Markdown / TXT / PDF / Word 上传、列表、删除、同名替换
- 问答：RAG 引用、多轮隔离、SSE 流式、拒答、引用原文定位与高亮
- 工具：员工 / 考勤 / 订单 / 当前时间，模型通过 function calling 自主选择
- IM：钉钉 Stream + 飞书 WebSocket，均支持多轮与引用展示
- 后台：文档、备用聊天、日志、可观测性、设置五页
- 工程化：uv + .venv、pnpm、React 19、Tailwind v4、脚本统一、日志落盘、错误重试降级

## 最得意的一个决策

线上 Railway 作为唯一 IM 实例，本地默认关闭钉钉 / 飞书机器人，并用 `/api/im/status` + `/api/im/toggle` 做运行期开关。它直接解决了「本地和线上同时回复两条」这个最难看的稳定性问题，面试官现场体验的是线上真实机器人，而不是本地截图或录屏。

## 被迫的妥协

飞书回复从卡片降级为纯文本，换来长连接收发稳定、失败重试和同消息去重；引用来源用文件名与原文文本清晰呈现，暂不做飞书卡片点击跳转。文档解析只做文本层，扫描版 PDF 未做 OCR。

## 如果再给一周

优先做 Langfuse / OpenTelemetry 链路导出、管理员权限与审计、飞书卡片引用跳转，以及企业微信第三端；然后是文档切块策略的评测调优，让答案准确率更可量化。

## AI 使用情况

代码绝大部分由 AI 协助完成；关键逻辑（IM topic 注册、function calling 消息流、拒答判定、重复回复）均经人工核实并配套回归测试，详见 `AI_USAGE.md`。

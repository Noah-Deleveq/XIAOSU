# 自评

## 完成情况

按笔试要求完成公司内部 AI 助手「小苏」：

- **知识库问答**：MD / PDF / Word / TXT 上传解析，BM25 检索（jieba 分词），回答带引用出处；检索不到时明确拒答、绝不编造
- **工具调用**：function calling 模型自主决策，调员工 / 考勤 / 销售订单 / 时间等内部 API
- **多轮对话**：按 用户 + 会话 隔离上下文，支持指代（"他上周来上班几天"）
- **钉钉接入**：官方 Stream 模式长连接，无需公网 IP/域名；私聊、群聊 @ 唤起；回复带引用来源；异常友好兜底
- **飞书接入**：WebSocket 长连接，无需公网 URL/HTTPS/内网穿透，文本回复，与钉钉共用同一套问答引擎
- **Web 管理后台**：文档管理、备用聊天（含文件上传问答）、对话日志、可观测性、服务状态五页
- **工程化**：uv 依赖管理（禁 pip）、38 个自动化测试（Mock LLM，不依赖真实 API）、一键启动脚本、日志落盘

## 技术亮点

1. **钉钉 Stream 长连接**：免公网部署，本地即可完整演示，是本题最省事的 IM 接入方案
2. **function calling 自主决策**：模型自己判断「查文档还是调工具」，无需硬编码路由
3. **检索-问答分离**：BM25 本地索引（JSON 持久化），零外部服务依赖，数据可控
4. **测试不依赖真实 LLM**：FakeClient 注入 + 模拟真实钉钉消息的回归测试，CI 可稳定复现

## 不足与改进

- 鉴权：当前为内网演示版，未做用户体系与权限控制（生产需接入钉钉身份）
- 文档解析深度：复杂表格 / 扫描版 PDF 仅提取文本层
- 群聊在部分组织需管理员开通机器人接收配置；演示首选私聊
- 前端为管理后台形态，未做完整 IM 界面

## AI 使用情况

本项目代码绝大部分由 AI 协助完成；关键逻辑（钉钉 SDK 兼容与 topic 注册、function calling 消息流、拒答判定）均经人工核实并配套回归测试，详见 `AI_USAGE.md`。

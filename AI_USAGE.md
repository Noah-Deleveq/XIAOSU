# AI_USAGE.md

## 1. 用了哪些 AI 工具？分别用在哪些环节？

架构与任务拆解：我之前曾独立开发果一个项目“多Agent协作工作台”，把笔试要求丢给它，让它按 M0-M6 里程碑的方式拆解任务

agent工具：openai的codex（ccswitch接入deepseek大模型）、Reasonix

LLM：deepseekv4-flash、deepseekv4-pro

代码生成：每一步的骨架代码、接口、测试都由它生成，我再逐行检查关键路径

排障：钉钉、飞书不回复、消息被静默丢弃等问题，把日志和现象贴给它，让它给出排查方向

测试：deepseek+人工，它生成 pytest 用例（Mock LLM），我补"模拟真实钉钉消息"的回归测试锁住踩过的坑

文档：deepseek  README 架构图、验收对照表

## 2. 举一个具体的例子：prompt 是什么、哪里能用、哪里必须改？

我给 AI 的话大概是这样的：机器人已经连上 wss 了，控制台没报错，私聊 @ 它也没回复，日志里看不到消息进来，帮我查

AI 先让我检查 topic 注册，也让我看 handler 有没有抛异常。它最初写的代码是 client.register_callback_handler("chatbot", XiaoSuBot())，看起来和官方示例一样，但钉钉推送的 topic 实际是 /v1.0/im/bot/messages/get，不是 chatbot。所以每条消息都匹配不上，被静默丢掉了。

我改成 ChatbotMessage.TOPIC。为什么必须改，因为官方 README 的示例省略了真实 topic 值，只有 SDK 源码里的常量才是运行时实际用的值。

后来我把这个坑写进回归测试，防止以后再踩。
## 3. 有没有一次 AI 把我带沟里去的经历？怎么发现、怎么收拾的？

我上面有说过我用两个模型：v4-flash和v4-pro，在我更换LLM之后发现ai只能分析最近的对话，而对于之前的大部分记忆全都遗忘，导致要求不符合

之后我重新再把要求重新复述了一遍，以及相应的路径文件也全部重新让其进行分析

## 4. 我怎么验证 AI 生成的代码是对的？

1.先不接真实的api和接口，让agent用假的模型接口跑一边看看，检查有无bug，以及提到的要求是否达到

2.再接真实的ai跑一遍

3.我按照标准测试问题清单上的问题逐次在web端、钉钉、飞书上逐个询问检查，看是否达到要求

## 5. 如果让我再做一遍，会怎么调整 AI 的使用方式？

如果让我再做一遍，我会反过来做，比如先把测试写好，再写功能，省得写完才发现一堆 bug。

别让ai太信官方文档，早点去翻 SDK 源码，钉钉文档说的和实际不一样，坑了我大半天。

让 AI 写代码时把“为什么这么写”直接说清楚，别等我后面补。
